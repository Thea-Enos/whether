import React from "react";

const Section = () => (
  <section>
    <div>
      <div>
        This container is <strong>centered</strong> on desktop.
      </div>
    </div>
  </section>
);

export default Section;
